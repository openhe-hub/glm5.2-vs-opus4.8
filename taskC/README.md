# taskC contest package

- `public/PROMPT.txt`: contestant-visible prompt.
- `starter/solution/`: identical empty starting solution.
- `private_judge/`: organizer-only gate checks and blind-judge helpers; do not expose to contestant agents.

Contestant workspaces are generated under `workspaces/taskC/`.
